# 交接总文档 · 本地-云蜂群基础设施（HANDOFF v1.0 公开脱敏版）

> 读者：任何接手本体系的调用程序、新会话实例、技能创建者
> data_cutoff: 2026-08-29 ｜ 脱敏说明：本公开版不含固定域名、密钥、本地绝对路径与个人配置；完整版存私有库。
> 关联：CLOUD_SWARM_ARCH.md（云架构+排期）

## 一、体系总图（已建成 + 在建）

| 层 | 资产 | 状态 |
|---|---|---|
| 本地推理 | 本地小模型全量上卡（47 tok/s），OpenAI 兼容端点，Bearer 鉴权 | ✅ 运行中 |
| 公网入口 | 命名隧道固定域名 → 本机端点；边缘拦 Python 裸 UA（403），客户端须带浏览器 UA | ✅ 运行中 |
| 看门狗 | 单实例守护；活性以日志新鲜度为准（勿用进程名计数，会自匹配虚高） | ✅ 运行中 |
| ASR 试点 | SenseVoice CPU 22–25×RT 实测；双课程基线 | ✅ 已验证 |
| 两级纠错管线 | T1 词典 135 条 → T2 本地小模型二遍 → difflog 审计；已知错误消除 92%（最难样本） | ✅ 已验证 |
| 热词/词典资产 | 物理 322 条、数学 213 条、纠错词典 135 条（四级风险分级） | ✅ v1.x |
| 蜂群纪律 | 三角色链（批判→解构→重整）本机实测可持续，单链 ~18s 0 配额 | ✅ 已验证 |
| 云蜂群 | 编排层+ASR 生产层+任务队列上云（7 天排期，见 CLOUD_SWARM_ARCH.md） | ⏳ 待云到货 |
| 同步层 | 私有 discovery_log 实例 = 跨会话可查记录通道 | ✅ |

## 二、调用接口契约（任何调用程序遵守）

### 2.1 推理端点
- `POST {BASE}/v1/chat/completions`，Header：`Authorization: Bearer <key>`；公网访问必须加浏览器 User-Agent
- Body：`{"model":"local","messages":[...],"temperature":0.0,"max_tokens":N}`
- 能力边界：单槽串行（并发排队）；~47 tok/s；无视觉（多模态 handler 未接）
- BASE：本机 `http://127.0.0.1:8000`；公网域名见本地私有配置（不写进任何公开文档）

### 2.2 两级纠错管线
```
python two_tier_correct.py <in.txt> <out.txt> <difflog.txt> --domain physics [--skip-gemma]
```
- 输入：逐行文本（支持行首 `#NNN` 编号与时间戳前缀，自动剥离回贴）；单块文本自动按标点分句
- context 级词典规则仅在 `--domain physics` 启用（含合法词误判风险，通用语料禁用）
- `--skip-gemma` = 只跑 T1（无 GPU 环境的纯词典档）

### 2.3 ASR 转写
```
MODELSCOPE_CACHE=<ASCII路径> <venv-python> run_sv.py <in.wav> <out.txt> <out_meta.json>
```
- 铁律：模型缓存必须纯 ASCII 路径（sentencepiece 不识别中文路径）
- 音频规格：16kHz 单声道 PCM WAV
- 已知缺陷：句级时间戳不稳定（可能整段一文），下游须独立标点分句

## 三、技能接入规范（技能创建者遵守）

1. 创建即合规：version 三段制、触发词摘要、别名登记、索引自检
2. 方言/领域扩展零改码：新方言 = 一个热词文件 + 一行路由配置；新术语 = 词典增条目（先定风险级：empirical/safe/context/variant）
3. conf 词表：High=empirical（≥2 独立信源且≥1 官方原文核过）｜Medium=estimated｜Low=assumed｜Conflict=矛盾未裁决
4. 红线随任务传递：禁编造/禁外发/写类例外逐次批准/禁假装实装——子代理派单时必须写进任务书
5. 额度三定律：机械 token 下沉本机；长任务必磁盘快照；真问题走干净通道（预处理浓缩后再上交主模型）

## 四、数据资产地图

| 资产 | 同步状态 |
|---|---|
| 纠错词典 135 条 / 热词库 535 条 | 本地主份 |
| 四份两级产物+difflog 审计日志 | 本地主份 |
| 完整性清单（文件哈希） | 本地主份 |
| 技术结论摘要 | 私有 discovery_log 实例 ✅ 已同步 |
| 架构/排期/交接文档 | 本地 + 本公开库（脱敏版）+ 私有库（完整版） |
| 密钥/配置 | **仅本地，永不外发** |

## 五、外发纪律（双库策略）

- **公开库（本库）**：架构、方法论、排期模板、接口契约形态——经五道预审（范围封闭/时限明确/撤销路径/逃逸面/红线冲突）
- **私有库**：含真实域名与逻辑路径的完整版
- **永不外发**：代码、脚本、密钥、含用户名绝对路径、未脱敏日志原文

## 六、接手检查单（新实例 10 步）

1. 读三件套：本文件 + CLOUD_SWARM_ARCH.md + 方言矩阵文档
2. 验本机端点 200（无钥 401 为正常）
3. 带浏览器 UA 验隧道 200
4. 核验热词/词典/管线四资产存在
5. 抽 1 句跑两级管线（~2s）
6. 读 discovery_log 最新 10 条对齐上下文
7. 抽验完整性清单 1 个文件哈希
8. 检查看门狗日志新鲜度
9. 读全部 top3_likely_wrong open 项
10. 完成即续作，不返工已通过审计的结论

## top3_likely_wrong

1. 本交接假设接手环境与撰写时同构——环境漂移时命令需重核
2. 「OpenClaw 为编排首选」未经云端实测（conf: Medium）——以实际落地框架为准
3. discovery_log 同步为单通道摘要——以库内 count 核验为准
