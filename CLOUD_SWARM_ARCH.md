# 云蜂群架构（K3 极致集群逼近版）· 可行性实证 + ROI 自审计 + 部署排期（v1.0 公开脱敏版）

> data_cutoff: 2026-08-29 ｜ 脱敏说明：本公开版已移除固定域名、密钥引用、本地路径与个人配置；完整版存私有库。
> 诚实边界：商业 Agent Swarm 编排器（如 Kimi K2.5/K3 的 swarm 编排器）为厂商专有云端产品、未开源——本架构是"自托管逼近版"，不是任何厂商产品的本体。

## 一、可行性实证矩阵（全部本机实测，非纸面推演）

| 组件 | 实证 | 结论 |
|---|---|---|
| 蜂群角色纪律（4B 本机模型） | 三角色链（批判→解构→重整）实测：角色不崩、格式服从、产出真洞见；单链 ~18s、0 API 配额 | ✅ 角色链形态立得住 |
| 机械 token 免费化 | 两级 ASR 纠错管线 547 句全量跑通 0 配额；本地小模型 0.4–1.5s/句 | ✅ 已规模化验证 |
| 本机端点并发 | llama-cpp-python 单 Llama 实例单槽，并发请求串行化 | ⚠️ 并行需 --parallel 槽位（费显存）；角色链天然串行不受影响 |
| 云端 CPU ASR | SenseVoice CPU 实测 22–25×RT（14C 本机）；4核8G 折算 4–8×RT，55h 课程音频一夜跑完；2核4G 约 2–4×RT | ✅ 两档均可用 |
| 框架对自建端点兼容 | LangGraph/CrewAI/pydantic-ai/smolagents 全支持 base_url 指向自建 OpenAI 兼容端点（坑：base_url 止于 /v1；CrewAI 需 custom_openai=True；模型名按端点实际 ID 透传） | ✅ |
| 主模型接入 | 主流国产 LLM API 均为 OpenAI 兼容，可作终审层直连 | ✅ |

## 二、目标架构（四层）

```
┌─ 终审层：主模型 API（真问题/语义残错终审/关键裁决）——只出思考 token
├─ 编排层：云服务器 24/7 常驻
│    首选 OpenClaw（现成运行时 ~390MB，sessions_spawn 子代理，4核8G 可行）
│    备选 LangGraph（子代理并行+持久状态最强）/ CrewAI（角色分工最快）
│    回避：OpenAI Swarm（废弃）、AutoGen（维护模式，用 AG2 分叉）、MetaGPT（未修 CVE）、Dify 全栈（8G 偏紧）
├─ 推理层A：本机 GPU（经隧道回接）47 tok/s —— 角色链/机械 token/纠错二遍
├─ 推理层B：云端 CPU 常驻 SenseVoice（ASR）+ 小模型 CPU 慢速兜底（主链路全断时保命）
└─ ASR 生产层：FunASR wss-server（热词+2pass）／OpenAI 兼容 API／--enable-mcp 三层端点
```

## 三、ROI 自审计（¥300 预算档）

### 成本侧（2026-08 华为云 Flexus L 公开价）

| 选项 | 年付 | 续费 | 流量 | 裁决 |
|---|---|---|---|---|
| 2核4G5M（70GB 盘） | ¥188 | 同价（活动页明标） | 1000GB/月 | **预算内最优** |
| Flexus X 2核4G5M 不限流量 | ¥198 | 未标注 | 不限 | 免流量焦虑 |
| 4核8G6M（180GB 盘） | ¥328–388（活动波动） | 未标注续费同价，续费价不确定 | 1200GB/月 | 超预算+续费风险 |

### 价值侧（全部有实测锚点）

- 机械 token 免费化：单日 547 句纠错 + 3 角色蜂群链 0 配额；同等工作量走主模型 API 约 14 万 token（estimated）
- 24/7 端点：摆脱笔记本开机依赖，移动端/轮询器随时可达
- 55h 课程转写云端一夜跑完，避免数十万 token 级 API 消耗
- 蜂群纪律下沉：三角色审查下沉本机，每轮省主模型 3 次调用

**ROI 判定：¥188/年 = ¥0.52/天，每天一次 ≥1 分钟机械任务即回本——高 ROI 成立，前提是锁定续费同价。**

## 四、部署排期（T0=拿到云 IP+SSH 之日，共 7 天）

| 阶段 | 内容 | 验收门 |
|---|---|---|
| T0+0.5d | SSH 密钥加固、防火墙（仅必需端口）、swap 2G | 外扫仅预期端口 |
| T0+1d | ASR 环境 1:1 复刻（funasr + kaldi-native-fbank + 模型缓存 ASCII 路径） | 10min 样本 ≥2×RT |
| T0+2d | 任务队列+落盘规范（逐 P 队列、断点续跑） | 杀进程重启不丢任务 |
| T0+3d | 隧道回接本机 GPU + 编排层安装 + 角色链队列打通 | 云端发起→本机执行→回传 <60s |
| T0+4d | 托管服务组：轮询器迁移+看门狗+每日自检 | 24h 无断推 |
| T0+5d | ASR 生产三层端点（wss-server 热词/OpenAI API/MCP） | 热词文件加载实证 |
| T0+6d | 全链路演练：课程队列首批实跑+两级纠错云端复刻+终审通道 | 产物与本地基线一致 |
| T0+7d | 验收+文档归档+记录同步 | 排期逐项打勾 |

**关键路径 = T0+3 隧道回接**（frp 最大不确定项，备选 Caddy / cloudflared 命名隧道）。

## 五、风险登记

1. 2核4G 内存贴顶：SenseVoice 峰值 <3GB + 编排层 ~400MB——swap 2G 必配，CPU 兜底模型不装（装则必 OOM）
2. 隧道 token 泄露=内网端点暴露：token 入 600 权限 EnvironmentFile，端口仅绑 127.0.0.1 回源
3. 4核8G 续费价陷阱：三年 TCO 可能反超 2核4G 方案——下单前控制台续费页截图存证
4. 6M 峰值带宽 ≈0.75MB/s：音频输入分批夜传，文本产物回传无压力

## top3_likely_wrong

1. OpenClaw 云端适配未经实测——T0+3 若卡死降级 LangGraph（conf: Medium）
2. 2核4G 的 2–4×RT 为核心数线性折算（conf: estimated），以 T0+1 实测为准
3. 「续费同价」依据活动页快照，厂商保留调价权（conf: Medium）
